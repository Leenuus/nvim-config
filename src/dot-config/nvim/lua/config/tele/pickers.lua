local cursor = {
  theme = "cursor",
}

return {
  lsp_references = cursor,
  lsp_definitions = cursor,
  lsp_declarations = cursor,
  lsp_implementations = cursor,

  colorscheme = {
    theme = "dropdown",
    enable_preview = true,
  },
}
